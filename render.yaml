services:
  - type: web
    name: run-rabbit
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -k eventlet -w 1 backend.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
